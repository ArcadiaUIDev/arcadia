---
layout: ../../../layouts/DocsLayout.astro
title: Gauge Chart
description: Blazor gauge chart component for KPI dashboards with color thresholds, radial display, and configurable ranges.
---

# Gauge Chart

Radial gauge for KPI dashboards with color thresholds.

## Basic Usage

```razor
<ArcadiaGaugeChart Value="73" Min="0" Max="100"
                 Label="CPU Usage" Width="200" Height="160" />
```

## With Thresholds

```csharp
var thresholds = new List<GaugeThreshold>
{
    new() { Value = 0, Color = "var(--arcadia-color-success)" },
    new() { Value = 60, Color = "var(--arcadia-color-warning)" },
    new() { Value = 85, Color = "var(--arcadia-color-danger)" },
};
```

## Full-Circle Mode

Set `FullCircle="true"` for a complete 360-degree radial progress indicator — ideal for scores, ratings, and completion percentages:

```razor
<ArcadiaGaugeChart Value="68" Min="0" Max="100" Label="Progress"
                 Width="180" Height="180" FullCircle="true" />

<ArcadiaGaugeChart Value="3.8" Min="0" Max="5" Label="Rating"
                 Width="180" Height="180" FullCircle="true"
                 ValueFormatString="0.0" />
```

## Parameters

| Parameter | Type | Default | Description |
|---|---|---|---|
| `Value` | `double` | — | Current gauge value |
| `Min` | `double` | 0 | Minimum range |
| `Max` | `double` | 100 | Maximum range |
| `Height` | `double` | 200 | Chart height in pixels |
| `Width` | `double` | 300 | Chart width in pixels |
| `Title` | `string?` | null | Chart title |
| `Label` | `string?` | null | Label below value |
| `Thresholds` | `List<GaugeThreshold>?` | null | Color thresholds |
| `Color` | `string` | `var(--arcadia-color-primary)` | Default arc color when no threshold is matched |
| `TrackColor` | `string` | `var(--arcadia-color-border)` | Background track color |
| `StrokeWidth` | `double` | 20 | Arc thickness |
| `FullCircle` | `bool` | false | Full 360° or semi-circle |
| `ValueFormatString` | `string?` | null | Format for center value |
| `AriaLabel` | `string?` | null | Accessible description for screen readers |
| `TrackOpacity` | `double` | 0.3 | Opacity of the background track arc (0.0–1.0) |
| `AnimateOnLoad` | `bool` | true | Whether to animate on load |
| `AnimationDuration` | `int` | 800 | Animation duration in milliseconds |
